# Yummyland Candy — лендинг с конструктором боксов конфет

Готовый к публикации сайт магазина Yummyland Candy: главная страница — конструктор,
в котором покупатель выбирает размер бокса, набирает конфеты, задаёт «вайб» и допы,
а превью бокса и цена пересчитываются вживую.

Никакой сборки и зависимостей: чистые HTML + CSS + ES5-совместимый JS, шрифты и вся
графика лежат внутри репозитория. Можно открыть `index.html` двойным кликом или залить
папку на любой статик-хостинг. Для переноса на Shopify есть готовый пакет — см. ниже.

## Страницы

| Файл | Что это |
|---|---|
| `index.html` | Главная: герой + **конструктор бокса** (5 шагов), как это работает, популярные боксы, поводы, блок для офисов, отзывы, FAQ |
| `boxes.html` | Каталог готовых боксов + витрина всех конфет с фильтрами по категориям |
| `cart.html` | Корзина: позиции, количество, промокод, доставка, оформление (демо) |
| `about.html` | О компании, качество и состав, офисы/мероприятия, форма обратной связи |
| `faq.html` | FAQ, доставка, возвраты и гарантия |
| `404.html` | Страница «не найдено» |

## Конструктор

1. **Размер** — Small / Medium / Large / Party (4 / 7 / 10 / 15 слотов, $24.99–$69.99).
   При уменьшении размера лишние конфеты аккуратно срезаются.
2. **Конфеты** — 22 позиции с фильтрами (гамми, кислые, шоколад, орехи, freeze dried,
   классика, премиум). Премиум-позиции добавляют наценку к цене бокса.
3. **Вайб** — просто себе, подарок, день рождения, офис, киновечер, вечеринка,
   праздник, «удиви меня» (последний собирает случайный бокс сам).
4. **Допы** — открытка (бесплатно), стикеры, доп. порция конфет, конфета-сюрприз,
   премиум-упаковка, цвет бокса (5 цветов) + поле аллергий/пожеланий.
5. **Проверка** — сводка со ссылками «Edit» на нужный шаг и кнопка «в корзину».

Дополнительно:

- **Живое превью** справа — SVG-бокс перерисовывается под выбранные конфеты,
  наполненность и цвет; чипсы выбранных конфет с удалением в один клик.
- **Auto-build** — 5 готовых миксов (Sour Lover, Sweet & Fruity, Chocolate Lover,
  Party Mix, Random) заполняют бокс одной кнопкой.
- **Сохранение** — черновик бокса, сохранённые боксы и корзина живут в `localStorage`.
- **Готовые боксы** открываются в конструкторе через `index.html?box=<id>`
  (например `index.html?box=movie-night`) или добавляются в корзину сразу.
- Промокоды в корзине: `SWEET10` (−10%) и `YUMMY5` (−$5).
  Доставка $4.99, бесплатно от $50.

## Структура

```
index.html boxes.html cart.html about.html faq.html 404.html
robots.txt sitemap.xml                 генерируются tools/build-seo.js
assets/css/style.css                   дизайн-система (цвета, кнопки, карточки, адаптив)
assets/fonts/*.woff2                   Nunito + Baloo 2, latin-сабсеты, ~80 КБ
assets/img/                            favicon, apple-touch-icon, og.png
assets/js/config.js                    пути и выбор корзины — единственное место для правок при переезде
assets/js/art.js                       вся графика: SVG-конфеты, бокс, иконки, логотип
assets/js/data.js                      каталог: размеры, конфеты, вайбы, допы, готовые боксы, FAQ
assets/js/store.js                     расчёт цены бокса, корзина и черновик в localStorage
assets/js/site.js                      шапка, подвал, тосты
assets/js/pages.js                     переиспользуемые секции (как это работает, боксы, отзывы, FAQ)
assets/js/builder.js                   сам конструктор
assets/js/cart.js                      страница корзины
assets/js/shopify-cart.js              адаптер корзины Shopify (в статике не подключается)
shopify/                               готовый пакет для темы Shopify — см. shopify/README-SHOPIFY.md
tools/build-seo.js                     JSON-LD + sitemap.xml + robots.txt из данных каталога
tools/build-shopify.js                 сборка shopify/assets и products.csv из исходников
```

Весь контент — в `assets/js/data.js`: цены, конфеты, готовые боксы, отзывы, FAQ.
Правки текста и цен делаются там, в HTML лазить не нужно.

## Локальный запуск

```bash
python3 -m http.server 8000    # затем http://localhost:8000
```

## Публикация

Конфиги уже лежат в репозитории, сборка не нужна ни в одном варианте:

- **GitHub Pages** — `.github/workflows/pages.yml`. **Обязательный ручной шаг:**
  Settings → Pages → Build and deployment → Source: **GitHub Actions**.
  Из workflow это не включить — токен Actions умеет деплоить в Pages, но не создавать
  сам Pages-сайт (API отвечает `Resource not accessible by integration`).
  После переключения деплой идёт на каждый push.
- **Netlify** — `netlify.toml`. Либо перетащить папку на app.netlify.com/drop, либо подключить репозиторий.
- **Vercel** — `vercel.json`, `vercel --prod` или импорт репозитория.
- **Любой хостинг / S3** — просто скопировать файлы, это статика.

После выбора домена один раз прогнать:

```bash
SITE_URL=https://ваш-домен.com node tools/build-seo.js
```

— перепишет canonical/OG-ссылки, `sitemap.xml` и `robots.txt` под реальный адрес.

## Перенос на Shopify

Каталог `shopify/` — drop-in для темы Shopify 2.0 (Dawn и производные):
секция с конструктором, секция с маркетинговыми блоками, шаблон страницы,
заскоупенный CSS и `products.csv` для импорта товаров.

Схема простая: бокс = вариант товара по размеру, состав едет в line item properties,
платные допы и премиум-наценки добавляются отдельными позициями. Корзина и оплата — родные
шопифаевские, конструктор при этом остаётся тот же самый код.

Пошаговая инструкция: **[shopify/README-SHOPIFY.md](shopify/README-SHOPIFY.md)**.

```bash
node tools/build-shopify.js    # пересобрать shopify/assets и products.csv после правок
```

## Про картинки

Фотографий продукта в проекте нет — вся графика (конфеты, коробки, иконки, OG-картинка)
генерируется кодом в SVG (`assets/js/art.js`). Благодаря этому превью бокса реагирует на
выбор покупателя, ничего не весит и не требует CDN. Когда появятся реальные фото, проще
всего заменить вызовы `YL.candyTile()` и `YL.boxArt()` на `<img>` — данные о конфетах уже
лежат в `data.js`.

## Что не реализовано намеренно

Оформление заказа в статичной версии симулируется на клиенте: платежей, бэкенда и учёта
остатков нет, промокоды и стоимость доставки считаются в браузере. Принимать деньги так
нельзя — для этого и нужен Shopify (или другой чекаут), см. раздел выше.
