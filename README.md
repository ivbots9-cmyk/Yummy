# Yummyland — премиальные подарочные боксы с конфетами

Сайт одного продукта: **Ruby Signature Gift Box** — бордовая коробка
**12.75 × 6.5 × 3.75 in** с **шестью прозрачными ячейками** (3 × 2) и крышкой, внутри
которой напечатаны надпись под повод и **два полароида**. Покупатель выбирает повод,
наполняет шесть ячеек (готовой коллекцией или сам), решает, чьи фото будут в крышке,
добавляет ленту и открытку.

Никакой сборки: HTML + CSS + ES5-совместимый JS, шрифты и картинки лежат в репозитории.
`python3 -m http.server 8000` → http://localhost:8000.

## Страницы

| Файл | Что это |
|---|---|
| `index.html` | Герой → готовые боксы → **конструктор** → что внутри → поводы → как это работает → корпоративным → FAQ |
| `boxes.html` | Все коллекции с фильтром по семействам, «собери свой», витрина конфет, пакеты-рефиллы |
| `gifts.html` | Лендинг под рекламу: `?for=wedding` / `birthday` / `baby-shower`… меняет заголовок и подборку коллекций под повод |
| `about.html` | История, о конфетах, корпоративные заказы, контакты |
| `faq.html` | FAQ, доставка, возвраты |
| `cart.html` | Корзина: боксы с превью, рефиллы, промокод, итог |

## Конструктор — 3 шага

На широком экране слева живое превью открытого бокса, справа текущий шаг.
На телефоне превью сверху, а в шаге с конфетами есть липкий мини-лоток из 6 ячеек.

1. **Candy** — сверху готовые боксы (одно нажатие заполняет все 6 ячеек), ниже вся линейка
   с фильтрами по типу и по цвету. Тап по ячейке выбирает её, тап по конфете кладёт в неё
   (или в первую пустую). Дальше не пустит, пока не заполнены все 6.
2. **Lid & photos** — главная фишка, поэтому шаг сделан витриной:
   - **«Who's it for?»** — 8 поводов (по умолчанию Just Because). Повод задаёт надпись на крышке
     и то, какие **наши** фото мы положим, если клиент не загружает свои;
   - **как оформить крышку** — три карточки, у каждой живая картинка именно этой крышки:
     **свои фото +$5.99** (самая большая карточка, бейдж «Most loved»), **наши фото под повод**
     (бесплатно), **без фото** (бесплатно). Выбор обязателен — мы спрашиваем, а не решаем
     за клиента, но отказаться можно;
   - при «своих фото» — две загрузки, фото сразу появляются в превью; подписи под фото.
3. **Extras & checkout** — если своих фото нет, сверху мягкое напоминание с картинкой крышки
   и кнопкой «Add my photos» (можно просто пройти мимо). Лента **+$6.99** и открытка **+$4.99**:
   при включении превью переключается на вкладку **«How it arrives»** — закрытая коробка с лентой
   и открыткой с текстом клиента. Дальше состав, итог и «в корзину».

Над превью две вкладки: **Inside** (открытый бокс) и **How it arrives** (как коробку получат).

**Чего намеренно нет:** размеров, веса, лишнего совка, доплат за вкус. Лоток фиксированный,
коробка должна закрываться ровно — поэтому допы только те, что не меняют начинку.

Ссылки прямо в конструктор: `index.html?collection=rainbow-row#builder`,
`index.html?occasion=wedding#builder` (можно вместе).

## Цены

| Что | Цена |
|---|---|
| Бокс (любая коллекция или свой) | **$54.99**, доставка бесплатно |
| Свои фото в крышке | +$5.99 |
| Лента и бирка | +$6.99 |
| Открытка с текстом | +$4.99 |
| Пакет-рефилл 1 lb | $14.99–16.99 |

Экономика в комментарии к `YL.BOX` в `data.js`: 6 ячеек × ~6 oz ≈ 2¼ lb конфет
(~$25 по полочной цене) + коробка, лоток, печать ≈ $7–9. Допы — почти чистая маржа.
Вес ячейки (`cupOz`) — одно число, поменяйте, когда придёт реальный лоток.

## Каталог — `assets/js/data.js`

Всё редактируется там, в HTML ничего не зашито.

- **`YL.BOX`** — размер, число ячеек, колонок, вес ячейки, цена, SKU.
- **`YL.CANDIES`** — 47 позиций, порядок = порядок на витрине:
  - **Albanese гамми** (13 + 11 одноцветных мишек) — ядро линейки, у всех реальные фото
    с Shopify CDN;
  - **Albanese кислые** (4);
  - **Albanese шоколад** (5) + Rainbow Bites — **фото ещё нет**, карточки рисуют конфету кодом;
  - **другие**: Swedish Fish, Sour Patch Kids, Jolly Rancher (ассорти + 5 цветов);
  - **леденцы**: Starlight Mints, Butterscotch, Root Beer Barrels, Cinnamon, Lemon Drops —
    фото тоже нет.

  У каждой позиции: `label` (надпись на наклейке ячейки), `types`, `colors`, `swatch`,
  `brand`, `about`, `img`, `nuts` (помечается «contains nuts»).
  > ⚠️ Шоколад Albanese и леденцы вписаны по памяти — сайт Albanese из этой среды был
  > недоступен. Перед запуском сверить SKU и названия с тем, что реально закупается,
  > и проставить `handle` и `img`.
- **`YL.OCCASIONS`** — 8 поводов: надпись, строка, подписи, цвета заглушки, иконка,
  рекомендованная коллекция.
- **`YL.COLLECTIONS`** — 10 готовых боксов, у каждого 6 id в порядке «верхний ряд слева
  направо, потом нижний» (важно для цветовых историй):

  | Коллекция | Идея |
  |---|---|
  | The Signature Six | лучшее из гамми Albanese |
  | The Bear Bar | только мишки |
  | Rainbow Row | по цвету на ячейку: красный → фиолетовый |
  | Ruby & Sapphire | верхний ряд красный, нижний синий |
  | Ruby Romance | все оттенки красного и розового |
  | Pearl & Blush | белое и пудровое — свадьба, baby shower |
  | The Sour Edit | кислое |
  | Chocolate Lounge | шоколад Albanese |
  | Gold & Ruby | половина гамми, половина шоколад |
  | The Candy Counter | леденцы в обёртке |
- **`YL.EXTRAS`** — три платных допа. **`YL.PRODUCTS`** — пакеты-рефиллы.
- `YL.FAQ`, `YL.HOW`, `YL.TRUST`, `YL.ANNOUNCE` — тексты.
- `YL.REVIEWS` — **заглушки**, на страницы сейчас не выводятся. Появятся реальные отзывы —
  вписать и вызвать `YL.renderReviews`.

## Картинки: что есть и что сгенерировать

Уже в репозитории (нарезаны из мокапов бокса):
`hero-box.webp/jpg` (герой), `box-open.webp` (бокс изнутри), `box-closed.webp`,
`box-wrapped.webp` (с лентой — картинка допа), `card.webp` (открытка), `box-insert.webp`
(вкладыш), `og.jpg` (превью ссылки), `lid/just-because-1/2.webp`.

**Нужно сгенерировать** — пока файла нет, на его месте аккуратная заглушка в цветах повода,
ничего не ломается:

| Файл | Что |
|---|---|
| `assets/img/lid/<повод>-1.webp`, `-2.webp` | по 2 фото на повод (квадрат ~600 px): `birthday`, `wedding`, `baby-shower`, `best-friend`, `love`, `thank-you`, `congrats` (для `just-because` уже есть) |
| `img` у 11 позиций без фото | шоколад Albanese, Rainbow Bites, 5 леденцов — фото россыпью |

Карточки коллекций рисуют бокс кодом из реальных шести ячеек, так что фото коллекций
не обязательны.

## Структура

```
index.html boxes.html gifts.html about.html faq.html cart.html 404.html
assets/css/style.css        дизайн-система: светлый фон, рубиновый цвет коробки, жёлтые кнопки; Baloo 2 + Nunito
                            (Great Vibes — только на нарисованной крышке, как на настоящей коробке)
assets/fonts/*.woff2        шрифты локально
assets/img/                 фото бокса, крышки по поводам, og, иконки
assets/js/config.js         пути, корзина, YL.img() — меняется при переезде
assets/js/photos.js         генерится tools/fetch-photos.js (локальные копии фото конфет)
assets/js/art.js            рисованные конфеты, превью бокса (YL.giftBox), иконки
assets/js/data.js           каталог
assets/js/store.js          модель бокса, цена, корзина/черновик в localStorage
assets/js/site.js           шапка, подвал, тосты, анонс-бар
assets/js/pages.js          секции: коллекции, поводы, как это работает, витрина, рефиллы, FAQ
assets/js/builder.js        конструктор
assets/js/cart.js           корзина
assets/js/analytics.js      события GA4 / Meta / TikTok
assets/js/shopify-cart.js   адаптер корзины Shopify (в статике не подключается)
shopify/                    пакет для темы — см. shopify/README-SHOPIFY.md
tools/build-seo.js          JSON-LD, sitemap.xml, robots.txt из каталога
tools/build-shopify.js      сборка shopify/assets и products.csv
tools/fetch-photos.js       качает фото конфет с CDN в assets/img/candy/
tools/stamp-assets.js       ?v=<хеш> на css/js против смешанного кеша
```

## Публикация

- **GitHub Pages** — `.github/workflows/pages.yml` (репозиторий public, Settings → Pages →
  Source: GitHub Actions). Workflow сам качает фото конфет и ставит хеши на css/js.
- **Netlify / Vercel** — конфиги в корне.
- После выбора домена: `SITE_URL=https://домен node tools/build-seo.js`.

Фото конфет: `node tools/fetch-photos.js` (скачать), `--check`, `--restore`.

## Перед запуском рекламы

1. Сгенерировать фото крышек по поводам и недостающие фото конфет (таблица выше).
2. Сверить шоколад и леденцы с реальной закупкой (см. ⚠️ выше).
3. Проверить `cupOz` и цену на реальном лотке.
4. Отзывы — только настоящие.
5. Пиксели GA4 / Meta / TikTok в `<head>` — события уже летят (`analytics.js`).
6. Terms / Privacy — настоящие тексты (сейчас ссылки ведут на FAQ).
7. Приём денег — через Shopify (`shopify/README-SHOPIFY.md`); в статике чекаут симулируется.
