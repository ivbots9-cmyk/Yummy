/* Generated by tools/build-shopify.js from assets/css/style.css — do not edit here. */
/* Every rule is scoped to .yummyland so the theme keeps its own styling. */
@font-face { font-family: 'Nunito'; src: url({{ 'yummyland-nunito.woff2' | asset_url }}) format('woff2'); font-weight: 400 900; font-display: swap; }
@font-face { font-family: 'Baloo 2'; src: url({{ 'yummyland-baloo2.woff2' | asset_url }}) format('woff2'); font-weight: 400 800; font-display: swap; }
@font-face { font-family: 'Great Vibes'; src: url({{ 'yummyland-greatvibes.woff2' | asset_url }}) format('woff2'); font-weight: 400; font-display: swap; }
:root {
  --ruby: #a0153e;
  --ruby-2: #b81d48;
  --ruby-deep: #7d0f30;
  --ruby-ink: #3b0a18;
  --pink: #ff4f8b;
  --gold: #ffc93c;
  --gold-2: #ffd97a;
  --gold-deep: #b7801a;
  --cream: #fffaf7;
  --paper: #fff1ee;
  --blush: #ffe3ea;
  --ink: #231419;
  --ink-70: #574349;
  --ink-50: #86737a;
  --line: rgba(160, 21, 62, .13);
  --line-gold: rgba(160, 21, 62, .25);
  --white: #ffffff;

  --serif: 'Baloo 2', 'Nunito', system-ui, sans-serif;
  --script: 'Great Vibes', 'Snell Roundhand', cursive;
  --sans: 'Nunito', 'Segoe UI', system-ui, -apple-system, sans-serif;

  --r: 14px;
  --r-lg: 20px;
  --shadow: 0 2px 6px rgba(160, 21, 62, .06), 0 12px 30px -14px rgba(160, 21, 62, .25);
  --shadow-lg: 0 26px 60px -26px rgba(90, 10, 35, .45);
  --wrap: 1200px;
  --ease: cubic-bezier(.2, .7, .2, 1);
}
.yummyland *, .yummyland *::before, .yummyland *::after { box-sizing: border-box; }
.yummyland { -webkit-text-size-adjust: 100%; scroll-behavior: smooth; overflow-x: clip; }
.yummyland {
  margin: 0; background: var(--cream); color: var(--ink);
  font: 400 16px/1.6 var(--sans); letter-spacing: .01em;
  overflow-x: hidden;
  overflow-x: clip;
}
.yummyland img, .yummyland svg { max-width: 100%; display: block; }
.yummyland img { height: auto; }
.yummyland a { color: inherit; }
.yummyland button, .yummyland input, .yummyland textarea { font: inherit; color: inherit; }
.yummyland button { cursor: pointer; }
.yummyland h1, .yummyland h2, .yummyland h3, .yummyland h4 { font-family: var(--serif); font-weight: 800; line-height: 1.08; margin: 0; letter-spacing: -.01em; text-wrap: balance; }
.yummyland p { margin: 0; }
.yummyland sup { font-size: .45em; vertical-align: .9em; margin-left: .05em; }
.yummyland .sr-only { position: absolute !important; width: 1px; height: 1px; overflow: hidden; clip: rect(0 0 0 0); white-space: nowrap; }
.yummyland .wrap { max-width: var(--wrap); margin: 0 auto; padding: 0 20px; }
.yummyland .center { text-align: center; }
.yummyland .muted { color: var(--ink-50); }
.yummyland .small { font-size: 13px; }
.yummyland .script { font-family: var(--script); font-weight: 400; }
body.modal-open { overflow: hidden; }
.yummyland :focus-visible { outline: 2px solid var(--gold); outline-offset: 2px; }
.yummyland .eyebrow {
  display: inline-block; font: 800 13px/1.2 var(--sans); letter-spacing: .06em; text-transform: uppercase;
  color: var(--pink);
}
.yummyland .ico { width: 20px; height: 20px; flex: none; }
.yummyland .btn {
  display: inline-flex; align-items: center; justify-content: center; gap: 9px;
  min-height: 46px; padding: 0 24px; border-radius: 999px; border: 2px solid var(--ruby);
  background: var(--ruby); color: #fff; text-decoration: none;
  font: 800 15.5px/1 var(--sans);
  transition: background .2s var(--ease), color .2s, border-color .2s, transform .2s var(--ease), box-shadow .2s;
  white-space: nowrap;
}
.yummyland .btn:hover { background: var(--ruby-deep); border-color: var(--ruby-deep); }
.yummyland .btn:active { transform: translateY(1px); }
.yummyland .btn[disabled] { opacity: .45; pointer-events: none; }
.yummyland .btn .ico { width: 18px; height: 18px; }
.yummyland .btn--lg { min-height: 56px; padding: 0 30px; font-size: 17px; }
.yummyland .btn--sm { min-height: 40px; padding: 0 18px; font-size: 14px; }
.yummyland .btn--block { width: 100%; }
.yummyland .btn--gold {
  background: var(--gold); border-color: var(--gold); color: var(--ink);
  box-shadow: 0 6px 16px -8px rgba(183, 128, 26, .8);
}
.yummyland .btn--gold:hover { background: #ffbf1f; border-color: #ffbf1f; }
.yummyland .btn--line { background: #fff; color: var(--ruby); border-color: var(--line-gold); }
.yummyland .btn--line:hover { background: var(--blush); border-color: var(--ruby); color: var(--ruby-deep); }
.yummyland .btn--light { background: #fff; color: var(--ruby); border-color: var(--line-gold); }
.yummyland .btn--light:hover { background: var(--blush); border-color: var(--ruby); }
.yummyland .link-btn {
  background: none; border: 0; padding: 0; color: var(--ruby); text-decoration: underline; text-underline-offset: 3px;
  text-decoration-color: var(--line-gold); font-size: 14.5px; font-weight: 700; display: inline-flex; align-items: center; gap: 6px;
}
.yummyland .link-btn:hover { text-decoration-color: var(--ruby); }
.yummyland .link-btn .ico { width: 16px; height: 16px; }
.yummyland .chip {
  min-height: 36px; padding: 0 16px; border-radius: 999px; border: 1px solid var(--line);
  background: var(--white); font-size: 14.5px; font-weight: 700; color: var(--ink-70);
  transition: all .15s;
}
.yummyland .chip:hover { border-color: var(--ruby); color: var(--ruby); }
.yummyland .chip.is-on { background: var(--ruby); color: var(--cream); border-color: var(--ruby); }
.yummyland .filters { display: flex; flex-wrap: wrap; gap: 8px; }
.yummyland .filters--center { justify-content: center; margin-bottom: 24px; }
.yummyland .announce { background: var(--ruby); color: #fff; font-size: 14px; font-weight: 700; }
.yummyland .announce__in { position: relative; height: 38px; }
.yummyland .announce__item {
  position: absolute; inset: 0; display: flex; align-items: center; justify-content: center; gap: 10px;
  opacity: 0; transform: translateY(8px); transition: opacity .5s, transform .5s var(--ease); text-align: center;
}
.yummyland .announce__item .ico { width: 16px; height: 16px; }
.yummyland .announce__item.is-on { opacity: 1; transform: none; }
.yummyland .announce__item.is-out { opacity: 0; transform: translateY(-8px); }
.yummyland .announce__item a { color: var(--gold-2); text-underline-offset: 3px; }
.yummyland .header { position: sticky; top: 0; z-index: 40; background: rgba(255, 255, 255, .94); backdrop-filter: blur(10px); border-bottom: 1px solid var(--line); }
.yummyland .header__in { display: flex; align-items: center; gap: 28px; height: 72px; }
.yummyland .logo { display: flex; flex-direction: column; text-decoration: none; line-height: 1; }
.yummyland .logo__txt { font: 800 29px/1 var(--serif); letter-spacing: .01em; color: var(--ruby); }
.yummyland .logo__txt sup { color: var(--ruby); font-family: var(--sans); font-size: 9px; vertical-align: 1.8em; }
.yummyland .logo__sub { font: 700 12px/1 var(--sans); color: var(--ink-50); margin-top: 2px; }
.yummyland .nav { display: flex; gap: 26px; margin-left: auto; }
.yummyland .nav a { text-decoration: none; font-size: 15.5px; font-weight: 700; color: var(--ink-70); padding: 6px 0; border-bottom: 2px solid transparent; }
.yummyland .nav a:hover, .yummyland .nav a.is-active { color: var(--ruby); border-bottom-color: var(--pink); }
.yummyland .header__tools { display: flex; gap: 6px; }
.yummyland .icon-btn { position: relative; width: 42px; height: 42px; display: grid; place-items: center; border-radius: 50%; border: 0; background: none; color: var(--ruby); }
.yummyland .icon-btn:hover { background: var(--paper); }
.yummyland .icon-btn .ico { width: 22px; height: 22px; }
.yummyland .cart-count {
  position: absolute; top: 3px; right: 1px; min-width: 18px; height: 18px; padding: 0 5px; border-radius: 9px;
  background: var(--pink); color: #fff; font-size: 11px; font-weight: 800; display: grid; place-items: center;
}
.yummyland .cart-count[hidden] { display: none; }
.yummyland .cart-count.pop { animation: pop .4s var(--ease); }
@keyframes pop { 50% { transform: scale(1.35); } }
.yummyland .burger { display: none; }
.yummyland .hero {
  position: relative; color: var(--ink); overflow: hidden;
  background: radial-gradient(90% 120% at 85% 20%, #ffd6e2 0%, var(--blush) 35%, var(--cream) 75%);
}
.yummyland .hero__in { position: relative; display: grid; grid-template-columns: 1fr 1.1fr; gap: 48px; align-items: center; padding-top: 56px; padding-bottom: 56px; }
.yummyland .hero h1 { font-size: clamp(40px, 5vw, 66px); margin: 12px 0 0; }
.yummyland .hero h1 em { font-style: normal; color: var(--ruby); display: block; }
.yummyland .hero__lede { max-width: 470px; color: var(--ink-70); font-size: 18px; margin: 16px 0 26px; }
.yummyland .hero__cta { display: flex; flex-wrap: wrap; gap: 12px; }
.yummyland .hero__facts { display: flex; flex-wrap: wrap; gap: 28px; margin-top: 30px; }
.yummyland .hero__facts div { font-size: 14px; font-weight: 700; color: var(--ink-50); }
.yummyland .hero__facts b { display: block; font: 800 26px/1.1 var(--serif); color: var(--ruby); }
.yummyland .hero__photo { position: relative; }
.yummyland .hero__photo img { width: 100%; border-radius: 26px; box-shadow: var(--shadow-lg); }
.yummyland .hero__stamp {
  position: absolute; left: -18px; bottom: 26px; width: 112px; height: 112px; border-radius: 50%;
  display: grid; place-items: center; text-align: center; padding: 12px;
  background: var(--gold); color: var(--ink); box-shadow: var(--shadow);
  font: 800 18px/1.05 var(--serif); transform: rotate(-8deg);
}
.yummyland .hero__stamp small { display: block; font: 700 11px/1.2 var(--sans); margin-top: 4px; }
.yummyland .trust { display: grid; grid-template-columns: repeat(4, 1fr); border-top: 1px solid var(--line); border-bottom: 1px solid var(--line); background: var(--white); }
.yummyland .trust__item { display: flex; align-items: center; gap: 14px; padding: 22px 20px; color: var(--ruby); }
.yummyland .trust__item + .trust__item { border-left: 1px solid var(--line); }
.yummyland .trust__item .ico { width: 28px; height: 28px; color: var(--pink); }
.yummyland .trust__item span { font-size: 13px; color: var(--ink-50); line-height: 1.35; }
.yummyland .trust__item b { display: block; font: 800 15px/1.3 var(--sans); color: var(--ruby); margin-bottom: 2px; }
.yummyland .section { padding: 96px 0; }
.yummyland .section--paper { background: var(--paper); }
.yummyland .section--ruby { background: var(--blush); color: var(--ink); }
.yummyland .section__head { text-align: center; max-width: 640px; margin: 0 auto 48px; }
.yummyland .section__head h2 { font-size: clamp(32px, 3.8vw, 48px); margin-top: 8px; }
.yummyland .section__head h2 em { font-style: normal; color: var(--ruby); }
.yummyland .section__head p { color: var(--ink-50); margin-top: 14px; font-size: 16.5px; }
.yummyland .divider { display: none; }
.yummyland .page-head { padding: 64px 0 44px; text-align: center; background: var(--blush); border-bottom: 1px solid var(--line); }
.yummyland .page-head h1 { font-size: clamp(38px, 4.6vw, 58px); margin-top: 10px; }
.yummyland .page-head h1 em { font-style: normal; color: var(--ruby); }
.yummyland .page-head p { max-width: 620px; margin: 16px auto 0; color: var(--ink-50); font-size: 17px; }
.yummyland .page-head .hero__cta { justify-content: center; margin-top: 26px; }
.yummyland .gbox {
  
  --ruby: #7a1128; --gold: #c9a15a; --gold-2: #e6c98f; --serif: 'Baloo 2', 'Nunito', sans-serif;
  container-type: inline-size; position: relative; width: 100%;
}
.yummyland .gbox__lid {
  position: relative; display: grid; grid-template-columns: 1.05fr 2.1fr 1fr; align-items: center; gap: 3cqi;
  padding: 5cqi 5cqi 4cqi; border-radius: 1.6cqi 1.6cqi 0 0;
  background: linear-gradient(180deg, #8a1832, var(--ruby) 60%, #6c0f24);
  box-shadow: inset 0 0 0 .5cqi rgba(0, 0, 0, .12), inset 0 -2cqi 3cqi -1cqi rgba(0, 0, 0, .25);
  color: var(--gold-2);
}
.yummyland .gbox__lid::after { content: ''; position: absolute; inset: 1.8cqi; border: 1px solid rgba(230, 201, 143, .22); border-radius: 1cqi; pointer-events: none; }
.yummyland .gbox__script { font: 400 5.4cqi/1.02 var(--script); color: var(--gold-2); text-align: center; text-wrap: balance; }
.yummyland .gbox__script span { display: block; font-size: .7em; margin-top: 1cqi; }
.yummyland .gbox__side { font: 500 2.5cqi/1.35 var(--serif); letter-spacing: .14em; text-transform: uppercase; text-align: center; color: var(--gold-2); text-wrap: balance; border-left: 1px solid rgba(230, 201, 143, .35); padding-left: 2.4cqi; }
.yummyland .gbox__side span { display: block; font-size: 1.4em; margin-top: 1cqi; letter-spacing: 0; }
.yummyland .gbox__polas { display: flex; justify-content: center; gap: 2.6cqi; }
.yummyland .pola {
  position: relative; margin: 0; width: 48%; max-width: 20cqi; background: #fffdf8; padding: 1.1cqi 1.1cqi 0;
  box-shadow: 0 1cqi 2.2cqi rgba(0, 0, 0, .35); color: var(--ink);
}
.yummyland .pola--1 { transform: rotate(-3deg); }
.yummyland .pola--2 { transform: rotate(4deg) translateY(1.4cqi); }
.yummyland .pola__tape { position: absolute; top: -1.4cqi; left: 50%; width: 7cqi; height: 2.6cqi; transform: translateX(-50%) rotate(-2deg); background: rgba(236, 222, 196, .85); }
.yummyland .pola__img { display: block; aspect-ratio: 1 / 1; overflow: hidden; background: #e8dccb; }
.yummyland .pola__img img, .yummyland .pola__img svg, .yummyland .pola__ph, .yummyland .pola__ph svg { width: 100%; height: 100%; object-fit: cover; display: block; }
.yummyland .pola figcaption { font: 400 2.9cqi/1 var(--script); text-align: center; padding: 1.2cqi 0 1.5cqi; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.yummyland .pola figcaption span { font-family: var(--sans); font-size: .7em; }
.yummyland .gbox__promises { grid-column: 1 / -1; display: grid; grid-template-columns: repeat(4, 1fr); margin-top: 1.4cqi; }
.yummyland .gbox__promises span { display: flex; flex-direction: column; align-items: center; gap: .8cqi; font: 500 1.55cqi/1.2 var(--sans); letter-spacing: .14em; text-transform: uppercase; text-align: center; padding: 0 1cqi; }
.yummyland .gbox__promises span + span { border-left: 1px solid rgba(230, 201, 143, .3); }
.yummyland .gbox__promises .ico { width: 3.6cqi; height: 3.6cqi; }
.yummyland .gbox__base {
  position: relative; padding: 3.4cqi 3.4cqi 0; border-radius: 0 0 1.6cqi 1.6cqi;
  background: linear-gradient(180deg, #4d0918, #6a0f22 12%, var(--ruby) 100%);
  box-shadow: var(--shadow-lg);
}
.yummyland .gbox__cups { display: grid; gap: 2cqi; }
.yummyland .gbox__front { display: flex; flex-direction: column; align-items: center; padding: 3cqi 0 3.4cqi; color: var(--gold-2); }
.yummyland .gbox__front b { font: 800 5.4cqi/1 var(--serif); letter-spacing: .06em; background: linear-gradient(180deg, #f1dcaa, #c9a15a 60%, #a8823d); -webkit-background-clip: text; background-clip: text; color: transparent; }
.yummyland .gbox__front b sup { font-size: .3em; vertical-align: 1.6em; }
.yummyland .gbox__front span { font: 500 1.8cqi/1 var(--serif); letter-spacing: .3em; text-transform: uppercase; margin-top: 1.2cqi; }
.yummyland .cup {
  position: relative; display: block; width: 100%; aspect-ratio: 4 / 3; padding: 0; margin: 0;
  border-radius: 1.8cqi; border: .35cqi solid rgba(255, 255, 255, .55);
  background: linear-gradient(160deg, rgba(255, 255, 255, .28), rgba(255, 255, 255, .06));
  box-shadow: inset 0 0 0 .3cqi rgba(255, 255, 255, .25), 0 .8cqi 1.6cqi rgba(0, 0, 0, .35);
  overflow: hidden; color: var(--cream); font: inherit; text-align: center;
  transition: transform .2s var(--ease), box-shadow .2s;
}
.yummyland button.cup:hover { transform: translateY(-2px); }
.yummyland .cup__fill { position: absolute; inset: .9cqi; border-radius: 1.2cqi; overflow: hidden; }
.yummyland .cup__fill img, .yummyland .cup__fill svg { width: 100%; height: 100%; object-fit: cover; }
.yummyland .cup::after { content: ''; position: absolute; inset: 0; background: linear-gradient(155deg, rgba(255, 255, 255, .35) 0 18%, transparent 30% 70%, rgba(255, 255, 255, .12)); pointer-events: none; }
.yummyland .cup__label {
  position: absolute; left: 50%; top: 50%; transform: translate(-50%, -50%); z-index: 1;
  width: 52%; aspect-ratio: 1; border-radius: 50%; display: flex; flex-direction: column; align-items: center; justify-content: center;
  background: var(--ruby); border: .3cqi solid rgba(230, 201, 143, .8); box-shadow: 0 .4cqi 1cqi rgba(0, 0, 0, .35);
  padding: 1cqi; color: var(--cream);
}
.yummyland .cup__label small { font: 600 1.05cqi/1 var(--sans); letter-spacing: .14em; text-transform: uppercase; color: var(--gold-2); }
.yummyland .cup__label b { font: 600 1.6cqi/1.08 var(--sans); letter-spacing: .04em; overflow-wrap: anywhere; max-width: 100%; text-transform: uppercase; margin-top: .5cqi; text-wrap: balance; }
.yummyland .cup__label i { width: 1.3cqi; height: 1.3cqi; border-radius: 50%; margin-top: .6cqi; border: 1px solid rgba(255, 255, 255, .6); }
.yummyland .cup--empty { border-style: dashed; border-color: rgba(230, 201, 143, .55); background: rgba(0, 0, 0, .14); display: grid; place-items: center; }
.yummyland .cup__plus { display: grid; place-items: center; color: var(--gold-2); }
.yummyland .cup__plus .ico { width: 5cqi; height: 5cqi; }
.yummyland .cup__hint { position: absolute; bottom: 1.4cqi; left: 0; right: 0; font-size: 1.7cqi; letter-spacing: .16em; text-transform: uppercase; color: rgba(230, 201, 143, .8); }
.yummyland .cup.is-selected { border-color: var(--gold-2); box-shadow: 0 0 0 .7cqi var(--gold), 0 1cqi 2cqi rgba(0, 0, 0, .4); animation: glow 1.6s ease-in-out infinite; }
@keyframes glow { 50% { box-shadow: 0 0 0 1.1cqi rgba(201, 161, 90, .55), 0 1cqi 2cqi rgba(0, 0, 0, .4); } }
.yummyland .gbox--compact .gbox__lid { grid-template-columns: 1fr 2fr 1fr; padding: 4.2cqi 4cqi 4.6cqi; }
.yummyland .gbox--compact .gbox__base { box-shadow: var(--shadow); }
.yummyland .builder { padding: 64px 0 96px; background: var(--paper); border-top: 1px solid var(--line); }
.yummyland .builder__head { text-align: center; margin-bottom: 30px; }
.yummyland .builder__head h2 { font-size: clamp(32px, 4vw, 48px); margin-top: 6px; }
.yummyland .builder__head h2 em { font-style: normal; color: var(--ruby); }
.yummyland .builder__head p { color: var(--ink-50); margin-top: 8px; font-weight: 600; }
.yummyland .bsteps-wrap { position: sticky; top: 72px; z-index: 30; background: rgba(255, 241, 238, .95); backdrop-filter: blur(8px); border-bottom: 1px solid var(--line); margin-bottom: 36px; }
.yummyland .bsteps { list-style: none; margin: 0 auto; padding: 0; display: flex; justify-content: center; gap: 4px; max-width: 720px; }
.yummyland .bsteps li { flex: 1; }
.yummyland .bsteps__btn { width: 100%; display: flex; align-items: center; justify-content: center; gap: 10px; padding: 14px 6px; background: none; border: 0; border-bottom: 3px solid transparent; color: var(--ink-50); font-size: 15px; font-weight: 800; }
.yummyland .bsteps__n { width: 28px; height: 28px; border-radius: 50%; border: 2px solid var(--line-gold); display: grid; place-items: center; font: 800 14px/1 var(--sans); flex: none; }
.yummyland .bsteps__n .ico { width: 14px; height: 14px; }
.yummyland .bsteps__btn.is-on { color: var(--ruby); border-bottom-color: var(--ruby); }
.yummyland .bsteps__btn.is-on .bsteps__n { background: var(--ruby); border-color: var(--ruby); color: var(--cream); }
.yummyland .bsteps__btn.is-done { color: var(--ruby); }
.yummyland .bsteps__btn.is-done .bsteps__n { background: #2fb57c; border-color: #2fb57c; color: #fff; }
.yummyland .builder__grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(0, 1fr); gap: 48px; align-items: start; }
.yummyland .bprev { position: sticky; top: 140px; }
.yummyland .bprev__bar { display: flex; align-items: center; justify-content: space-between; gap: 16px; margin-top: 18px; padding: 16px 20px; background: var(--white); border: 1px solid var(--line); border-radius: var(--r-lg); }
.yummyland .bprev__label { display: block; font: 800 19px/1.1 var(--serif); color: var(--ruby); }
.yummyland .bprev__meta { display: block; font-size: 12.5px; color: var(--ink-50); letter-spacing: .04em; margin-top: 3px; }
.yummyland .bprev__price { font: 800 30px/1 var(--serif); color: var(--ruby); }
.yummyland .bprev__sel { margin-top: 12px; padding: 12px 16px; border-radius: var(--r); background: rgba(201, 161, 90, .14); border: 1px solid var(--line-gold); font-size: 14px; color: var(--ruby-deep); }
.yummyland .bstep { min-height: 360px; }
.yummyland .bstep__title { font-size: clamp(28px, 3vw, 38px); margin: 6px 0 8px; color: var(--ink); }
.yummyland .bstep__lede { color: var(--ink-50); margin-bottom: 28px; max-width: 560px; }
.yummyland .sub { font: 800 18px/1.3 var(--serif); color: var(--ink); margin: 28px 0 12px; }
.yummyland .sub .muted { font: 600 14px var(--sans); }
.yummyland .bnav { display: flex; justify-content: space-between; align-items: center; gap: 12px; margin-top: 36px; padding-top: 24px; border-top: 1px solid var(--line); }
.yummyland .occ-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 8px; }
.yummyland .occ {
  display: flex; flex-direction: column; align-items: center; gap: 6px; padding: 12px 6px; text-align: center;
  background: var(--white); border: 2px solid var(--line); border-radius: var(--r); transition: all .15s;
}
.yummyland .occ:hover { border-color: var(--pink); }
.yummyland .occ.is-on { border-color: var(--ruby); background: var(--blush); }
.yummyland .occ__ico { width: 36px; height: 36px; border-radius: 50%; display: grid; place-items: center; background: var(--paper); color: var(--ruby); }
.yummyland .occ.is-on .occ__ico { background: var(--ruby); color: #fff; }
.yummyland .occ b { font: 800 14px/1.15 var(--sans); color: var(--ink); }
.yummyland .occ-note { margin-top: 10px; }
.yummyland .suggest { display: grid; grid-template-columns: auto 1fr; gap: 10px 16px; align-items: center; margin-top: 22px; padding: 18px; background: var(--white); border: 1px dashed var(--gold); border-radius: var(--r-lg); }
.yummyland .suggest p { font-size: 14px; color: var(--ink-70); }
.yummyland .suggest .btn { grid-column: 1 / -1; justify-self: start; }
.yummyland .suggest__dots, .yummyland .colpick__dots { display: grid; grid-template-columns: repeat(3, 12px); gap: 3px; }
.yummyland .suggest__dots i, .yummyland .colpick__dots i { width: 12px; height: 12px; border-radius: 3px; box-shadow: inset 0 0 0 1px rgba(0, 0, 0, .08); }
.yummyland .views { display: inline-flex; gap: 4px; padding: 4px; margin-bottom: 12px; border-radius: 999px; background: #fff; border: 1px solid var(--line); }
.yummyland .views__btn { border: 0; background: none; padding: 8px 16px; border-radius: 999px; font-weight: 800; font-size: 14px; color: var(--ink-50); }
.yummyland .views__btn.is-on { background: var(--ruby); color: #fff; }
.yummyland .closed { position: relative; padding: 28px; border-radius: var(--r-lg); background: radial-gradient(80% 80% at 50% 40%, #fff, var(--blush)); }
.yummyland .closed > img { width: 100%; border-radius: 14px; box-shadow: var(--shadow-lg); }
.yummyland .closed__card { position: absolute; right: 18px; bottom: 18px; width: 30%; transform: rotate(5deg); box-shadow: var(--shadow); border-radius: 6px; overflow: hidden; background: #fff; }
.yummyland .closed__card img { width: 100%; }
.yummyland .closed__card i { display: block; padding: 6px 8px; font: 700 11px/1.3 var(--sans); color: var(--ink-70); font-style: normal; }
.yummyland .closed__tags { position: absolute; left: 18px; top: 16px; display: flex; gap: 6px; }
.yummyland .closed__tags b { display: inline-flex; align-items: center; gap: 5px; padding: 5px 10px; border-radius: 999px; background: var(--ruby); color: #fff; font-size: 12.5px; }
.yummyland .closed__tags .ico { width: 15px; height: 15px; }
.yummyland .gbox--lid .gbox__lid { border-radius: 1.6cqi; }
.yummyland .gbox__polas--none { display: grid; place-items: center; min-height: 26cqi; color: rgba(230, 201, 143, .5); }
.yummyland .gbox__polas--none .ico { width: 10cqi; height: 10cqi; }
.yummyland .pola__yours { width: 100%; height: 100%; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 1cqi;
  background: repeating-linear-gradient(45deg, #fbeff2 0 2cqi, #fff 2cqi 4cqi); color: #a0153e; font: 800 2.2cqi/1 'Nunito', sans-serif; }
.yummyland .pola__yours svg { width: 7cqi; height: 7cqi; }
.yummyland .lidopts { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
.yummyland .lidopt { position: relative; display: flex; flex-direction: column; padding: 0; text-align: left; overflow: hidden;
  background: #fff; border: 2px solid var(--line); border-radius: var(--r-lg); transition: border-color .15s, box-shadow .15s; }
.yummyland .lidopt:hover { border-color: var(--pink); }
.yummyland .lidopt.is-on { border-color: var(--ruby); box-shadow: 0 0 0 3px rgba(160, 21, 62, .12); }
.yummyland .lidopt--main { grid-column: 1 / -1; flex-direction: row; align-items: center; border-color: rgba(255, 79, 139, .45); background: linear-gradient(135deg, #fff, var(--blush)); }
.yummyland .lidopt__art { display: block; padding: 12px; background: var(--paper); }
.yummyland .lidopt--main .lidopt__art { flex: 0 0 58%; background: none; padding: 16px; }
.yummyland .lidopt__body { position: relative; display: flex; flex-direction: column; gap: 4px; padding: 12px 14px 14px 44px; }
.yummyland .lidopt--main .lidopt__body { padding: 16px 18px 16px 48px; }
.yummyland .lidopt__radio { position: absolute; left: 14px; top: 14px; width: 22px; height: 22px; border-radius: 50%; border: 2px solid var(--line-gold); background: #fff; }
.yummyland .lidopt--main .lidopt__radio { left: 18px; top: 18px; }
.yummyland .lidopt.is-on .lidopt__radio { border-color: var(--ruby); box-shadow: inset 0 0 0 5px #fff; background: var(--ruby); }
.yummyland .lidopt__body > b { font: 800 17px/1.15 var(--serif); }
.yummyland .lidopt--main .lidopt__body > b { font-size: 22px; }
.yummyland .lidopt__body > small { font-size: 13.5px; color: var(--ink-50); line-height: 1.45; }
.yummyland .lidopt__price { margin-top: 4px; font-weight: 800; color: var(--ruby); }
.yummyland .lidopt--main .lidopt__price { font-size: 18px; }
.yummyland .lidopt__badge { align-self: flex-start; margin-bottom: 4px; padding: 4px 12px; border-radius: 999px; background: var(--pink); color: #fff; font-size: 13px; font-weight: 800; }
.yummyland .upsell { display: grid; grid-template-columns: 200px 1fr; gap: 18px; align-items: center; margin-bottom: 18px; padding: 16px;
  border-radius: var(--r-lg); background: linear-gradient(135deg, var(--blush), #fff6d9); border: 2px dashed rgba(255, 79, 139, .45); }
.yummyland .upsell__title { display: block; font: 800 19px/1.2 var(--serif); }
.yummyland .upsell__text { display: block; font-size: 14px; color: var(--ink-70); margin: 4px 0 12px; }
.yummyland .colpick { display: flex; gap: 10px; overflow-x: auto; padding: 2px 2px 10px; scroll-snap-type: x mandatory; scrollbar-width: thin; }
.yummyland .colpick__btn {
  flex: 0 0 auto; scroll-snap-align: start; display: flex; flex-direction: column; align-items: flex-start; gap: 6px;
  min-width: 150px; padding: 14px; background: var(--white); border: 1px solid var(--line); border-radius: var(--r-lg); text-align: left;
}
.yummyland .colpick__btn:hover { border-color: var(--pink); }
.yummyland .colpick__btn.is-on { border-color: var(--ruby); box-shadow: 0 0 0 1px var(--ruby); }
.yummyland .colpick__btn b { font: 800 16px/1.1 var(--sans); }
.yummyland .colpick__btn small { font-size: 12.5px; font-weight: 700; color: var(--pink); }
.yummyland .swatches { display: flex; flex-wrap: wrap; align-items: center; gap: 8px; margin: 14px 0 20px; }
.yummyland .swatches__label { font-size: 14px; font-weight: 800; color: var(--ink-50); margin-right: 4px; }
.yummyland .swatch { width: 28px; height: 28px; border-radius: 50%; border: 2px solid var(--white); box-shadow: 0 0 0 1px var(--line); padding: 0; }
.yummyland .swatch:hover { box-shadow: 0 0 0 1px var(--gold); }
.yummyland .swatch.is-on { box-shadow: 0 0 0 2px var(--ruby); transform: scale(1.1); }
.yummyland .candies { display: grid; grid-template-columns: repeat(2, 1fr); gap: 14px; }
.yummyland .candy { position: relative; display: flex; flex-direction: column; background: var(--white); border: 1px solid var(--line); border-radius: var(--r-lg); overflow: hidden; transition: border-color .2s, box-shadow .2s; }
.yummyland .candy:hover { border-color: var(--line-gold); box-shadow: var(--shadow); }
.yummyland .candy.is-in { border-color: var(--ruby); }
.yummyland .candy__art { display: block; width: 100%; aspect-ratio: 4 / 3; padding: 0; border: 0; background: var(--paper); overflow: hidden; }
.yummyland .candy__art img, .yummyland .candy__art svg { width: 100%; height: 100%; object-fit: cover; transition: transform .5s var(--ease); }
.yummyland .candy:hover .candy__art img, .yummyland .candy:hover .candy__art svg { transform: scale(1.04); }
.yummyland .candy__body { display: flex; flex-direction: column; gap: 4px; padding: 14px 14px 16px; flex: 1; }
.yummyland .candy__brand { font-size: 12px; font-weight: 800; color: var(--pink); }
.yummyland .candy__body b { font: 800 17px/1.15 var(--serif); }
.yummyland .candy__body p { font-size: 13px; color: var(--ink-50); line-height: 1.45; flex: 1; margin-bottom: 10px; }
.yummyland .candy__body .btn { align-self: flex-start; }
.yummyland .candy__tag, .yummyland .candy__count { position: absolute; top: 10px; z-index: 1; padding: 4px 10px; border-radius: 999px; font-size: 12px; font-weight: 800; }
.yummyland .candy__tag { left: 10px; background: var(--pink); color: #fff; }
.yummyland .candy__count { right: 10px; background: var(--ruby); color: #fff; }
.yummyland .cups-tools { margin-top: 18px; }
.yummyland .collage { position: relative; display: grid; grid-template-columns: 1fr 1fr; grid-template-rows: 1fr 1fr; width: 100%; height: 100%; }
.yummyland .collage__cell { overflow: hidden; }
.yummyland .collage__cell img, .yummyland .collage__cell svg { width: 100%; height: 100%; object-fit: cover; }
.yummyland .collage__more { position: absolute; right: 8px; bottom: 8px; padding: 3px 8px; border-radius: 999px; background: var(--ruby); color: var(--gold-2); font-size: 12px; }
.yummyland .tray-mobile { display: none; }
.yummyland .modal { position: fixed; inset: 0; z-index: 90; display: grid; place-items: center; padding: 20px; background: rgba(42, 20, 24, .55); backdrop-filter: blur(4px); animation: fade .2s; }
@keyframes fade { from { opacity: 0; } }
.yummyland .modal__box { position: relative; width: min(640px, 100%); max-height: 88vh; overflow: auto; background: var(--cream); border-radius: var(--r-lg); padding: 28px; box-shadow: var(--shadow-lg); }
.yummyland .modal__title { font-size: 28px; margin: 6px 0 6px; }
.yummyland .modal__close { position: absolute; top: 12px; right: 12px; width: 40px; height: 40px; border-radius: 50%; border: 0; background: var(--paper); display: grid; place-items: center; color: var(--ruby); }
.yummyland .gpick { display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; margin-top: 20px; }
.yummyland .gpick__btn { position: relative; display: flex; flex-direction: column; align-items: center; gap: 6px; padding: 0 0 12px; border: 1px solid var(--line); border-radius: var(--r-lg); background: var(--white); overflow: hidden; }
.yummyland .gpick__btn:hover { border-color: var(--ruby); }
.yummyland .gpick__art { display: block; width: 100%; aspect-ratio: 1; overflow: hidden; }
.yummyland .gpick__art img, .yummyland .gpick__art svg { width: 100%; height: 100%; object-fit: cover; }
.yummyland .gpick__btn i { width: 14px; height: 14px; border-radius: 50%; margin-top: 4px; box-shadow: 0 0 0 2px var(--white), 0 0 0 3px var(--line); }
.yummyland .gpick__btn b { font: 800 14px/1.1 var(--sans); }
.yummyland .choice { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
.yummyland .choice__opt { position: relative; display: flex; flex-direction: column; gap: 4px; text-align: left; padding: 16px; border: 1px solid var(--line); border-radius: var(--r-lg); background: var(--white); }
.yummyland .choice__opt:hover { border-color: var(--gold); }
.yummyland .choice__opt.is-on { border-color: var(--ruby); box-shadow: 0 0 0 1px var(--ruby); }
.yummyland .choice__opt b { font: 800 17px/1.15 var(--serif); margin-top: 8px; }
.yummyland .choice__opt small { color: var(--ink-50); font-size: 13px; line-height: 1.4; }
.yummyland .choice__price { margin-top: 8px; font-size: 14px; font-weight: 800; color: var(--ruby); }
.yummyland .choice__pics { display: flex; gap: 8px; height: 92px; align-items: center; }
.yummyland .choice__pics--own { justify-content: center; background: var(--paper); border-radius: var(--r); color: var(--ruby); border: 1px dashed var(--line-gold); }
.yummyland .choice__pics--own .ico { width: 34px; height: 34px; }
.yummyland .mini-pola { width: 76px; padding: 5px 5px 14px; background: #fffdf8; box-shadow: 0 4px 10px rgba(0, 0, 0, .15); transform: rotate(-3deg); }
.yummyland .mini-pola + .mini-pola { transform: rotate(4deg); }
.yummyland .mini-pola img, .yummyland .mini-pola .pola__ph, .yummyland .mini-pola svg { width: 100%; aspect-ratio: 1; object-fit: cover; }
.yummyland .uploads { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; margin-top: 16px; }
.yummyland .upload { position: relative; display: grid; place-items: center; aspect-ratio: 1; border-radius: var(--r-lg); border: 1px dashed var(--gold); background: var(--white); cursor: pointer; overflow: hidden; }
.yummyland .upload:hover { background: #fffaf1; }
.yummyland .upload:focus-within { box-shadow: 0 0 0 2px var(--gold); }
.yummyland .upload__empty { display: flex; flex-direction: column; align-items: center; gap: 6px; color: var(--ruby); text-align: center; padding: 10px; }
.yummyland .upload__empty .ico { width: 30px; height: 30px; }
.yummyland .upload__empty b { font: 800 17px/1 var(--serif); }
.yummyland .upload__empty small { font-size: 12px; color: var(--ink-50); }
.yummyland .upload img { width: 100%; height: 100%; object-fit: cover; }
.yummyland .upload__change { position: absolute; left: 10px; bottom: 10px; display: inline-flex; gap: 6px; align-items: center; padding: 6px 10px; border-radius: 999px; background: rgba(58, 6, 19, .78); color: var(--cream); font-size: 12px; }
.yummyland .upload__change .ico { width: 14px; height: 14px; }
.yummyland .uploads + .muted { margin-top: 10px; }
.yummyland .fields { display: grid; gap: 12px; }
.yummyland .fields--2 { grid-template-columns: 1fr 1fr; }
.yummyland .field { display: flex; flex-direction: column; gap: 6px; }
.yummyland .field > span { font-size: 14px; font-weight: 800; color: var(--ink-70); }
.yummyland .field input, .yummyland .field textarea, .yummyland .promo input, .yummyland .subscribe input {
  width: 100%; min-height: 46px; padding: 11px 14px; border: 1px solid var(--line); border-radius: var(--r);
  background: var(--white); font-size: 15px; transition: border-color .15s;
}
.yummyland .field textarea { resize: vertical; line-height: 1.45; }
.yummyland .field input:focus, .yummyland .field textarea:focus, .yummyland .promo input:focus { outline: none; border-color: var(--gold); box-shadow: 0 0 0 3px rgba(201, 161, 90, .2); }
.yummyland .field__count { align-self: flex-end; font-size: 12px; color: var(--ink-50); }
.yummyland .fields + .muted { margin-top: 8px; }
.yummyland .touches { display: grid; gap: 12px; }
.yummyland .touch { display: grid; grid-template-columns: 140px 1fr; text-align: left; padding: 0; border: 1px solid var(--line); border-radius: var(--r-lg); background: var(--white); overflow: hidden; }
.yummyland .touch:hover { border-color: var(--gold); }
.yummyland .touch.is-on { border-color: var(--ruby); box-shadow: 0 0 0 1px var(--ruby); }
.yummyland .touch__img { display: block; background: var(--paper); }
.yummyland .touch__img img { width: 100%; height: 100%; object-fit: cover; }
.yummyland .touch__body { display: flex; flex-direction: column; gap: 4px; padding: 16px 18px; }
.yummyland .touch__ico { color: var(--pink); }
.yummyland .touch__body b { font: 800 18px/1.15 var(--serif); }
.yummyland .touch__body small { color: var(--ink-50); font-size: 13px; line-height: 1.45; }
.yummyland .touch__price { display: inline-flex; align-items: center; gap: 6px; margin-top: 6px; font-size: 14.5px; font-weight: 800; color: var(--ruby); }
.yummyland .touch__price .ico { width: 15px; height: 15px; }
.yummyland .cardform { margin-top: 6px; }
.yummyland .cardform .field { margin-top: 12px; }
.yummyland .included { list-style: none; margin: 18px 0 0; padding: 0; display: grid; grid-template-columns: 1fr 1fr; gap: 6px 16px; }
.yummyland .included li { display: flex; gap: 8px; align-items: flex-start; font-size: 14px; color: var(--ink-70); }
.yummyland .included .ico { width: 18px; height: 18px; color: #2fb57c; margin-top: 2px; }
.yummyland .review { margin: 0; border-top: 1px solid var(--line); }
.yummyland .review__row { display: grid; grid-template-columns: 110px 1fr auto; gap: 14px; padding: 16px 0; border-bottom: 1px solid var(--line); align-items: start; }
.yummyland .review dt { font-size: 11.5px; letter-spacing: .16em; text-transform: uppercase; color: var(--ink-50); padding-top: 3px; }
.yummyland .review dd { margin: 0; font-size: 15px; }
.yummyland .review__cups { list-style: none; margin: 0; padding: 0; display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
.yummyland .review__cups li { display: flex; align-items: center; gap: 10px; font-size: 14px; line-height: 1.2; }
.yummyland .review__cups small { display: block; font-size: 11px; color: var(--ink-50); letter-spacing: .06em; margin-top: 2px; }
.yummyland .candy-dot { flex: none; width: 38px; height: 38px; border-radius: 50%; overflow: hidden; box-shadow: 0 0 0 2px var(--white), 0 0 0 3px var(--line-gold); }
.yummyland .candy-dot img, .yummyland .candy-dot svg { width: 100%; height: 100%; object-fit: cover; }
.yummyland .lines { margin-top: 20px; }
.yummyland .line { display: flex; justify-content: space-between; gap: 16px; padding: 7px 0; font-size: 14.5px; color: var(--ink-70); }
.yummyland .line b { font-weight: 500; color: var(--ink); }
.yummyland .line--total { margin-top: 8px; padding-top: 14px; border-top: 1px solid var(--line); font-size: 15px; color: var(--ink); }
.yummyland .line--total b { font: 800 28px/1 var(--serif); color: var(--ruby); }
.yummyland .alert { margin-top: 18px; padding: 14px 16px; border-radius: var(--r); background: #fbeaea; border: 1px solid rgba(122, 17, 40, .25); color: var(--ruby-deep); font-size: 14px; }
.yummyland .review__cta { margin-top: 22px; }
.yummyland .review__cta .btn { width: 100%; }
.yummyland .cols { display: grid; grid-template-columns: repeat(3, 1fr); gap: 28px; }
.yummyland .col-card { display: flex; flex-direction: column; background: var(--white); border: 1px solid var(--line); border-radius: var(--r-lg); overflow: hidden; transition: box-shadow .25s, transform .25s var(--ease); }
.yummyland .col-card:hover { box-shadow: var(--shadow-lg); transform: translateY(-3px); }
.yummyland .col-card__art { display: block; padding: 22px; background: var(--paper); }
.yummyland .col-card__body { display: flex; flex-direction: column; gap: 8px; padding: 22px 24px 24px; flex: 1; border-top: 1px solid var(--line); }
.yummyland .col-card__body h3 { font-size: 24px; }
.yummyland .col-card__body p { color: var(--ink-50); font-size: 14px; line-height: 1.5; }
.yummyland .col-card__cups { list-style: none; padding: 0; margin: 6px 0 0; display: grid; grid-template-columns: 1fr 1fr; gap: 4px 12px; font-size: 12.5px; color: var(--ink-70); }
.yummyland .col-card__cups li { display: flex; align-items: center; gap: 7px; }
.yummyland .col-card__cups i, .yummyland .tags i { width: 9px; height: 9px; border-radius: 50%; flex: none; box-shadow: inset 0 0 0 1px rgba(0, 0, 0, .1); }
.yummyland .col-card__foot { display: flex; justify-content: space-between; align-items: center; gap: 10px; margin-top: auto; padding-top: 18px; flex-wrap: wrap; }
.yummyland .col-card__btns { display: flex; gap: 8px; }
.yummyland .price { font: 800 26px/1 var(--serif); color: var(--ruby); }
.yummyland .occs { display: grid; grid-template-columns: repeat(4, 1fr); gap: 20px; }
.yummyland .occ-card { display: flex; flex-direction: column; align-items: center; text-align: center; gap: 6px; padding: 26px 18px 24px; text-decoration: none; background: var(--white); border: 1px solid var(--line); border-radius: var(--r-lg); transition: all .25s var(--ease); }
.yummyland .occ-card:hover { border-color: var(--pink); transform: translateY(-3px); box-shadow: var(--shadow); }
.yummyland .occ-card__pola { width: 120px; padding: 7px 7px 20px; background: #fffdf8; box-shadow: 0 8px 18px rgba(0, 0, 0, .22); transform: rotate(-3deg); margin-bottom: 12px; }
.yummyland .occ-card:nth-child(even) .occ-card__pola { transform: rotate(3deg); }
.yummyland .occ-card__pola img, .yummyland .occ-card__pola .pola__ph, .yummyland .occ-card__pola svg { width: 100%; aspect-ratio: 1; object-fit: cover; }
.yummyland .occ-card__ico { color: var(--pink); }
.yummyland .occ-card b { font: 800 20px/1.1 var(--serif); }
.yummyland .occ-card__script { font: 600 14px/1.3 var(--sans); color: var(--ink-50); }
.yummyland .how { list-style: none; margin: 0; padding: 0; display: grid; grid-template-columns: repeat(3, 1fr); gap: 0; }
.yummyland .how__item { padding: 28px 24px 8px 0; }
.yummyland .how__item + .how__item { padding-left: 24px; border-left: 1px solid var(--line); }
.yummyland .how__n { display: grid; place-items: center; width: 48px; height: 48px; border-radius: 50%; background: var(--blush); font: 800 20px/1 var(--serif); color: var(--ruby); margin-bottom: 14px; }
.yummyland .how__item b { display: block; font: 800 20px/1.15 var(--serif); margin-bottom: 6px; }
.yummyland .how__item p { color: var(--ink-50); font-size: 14.5px; }
.yummyland .showcase { display: grid; grid-template-columns: 1fr 1fr; gap: 64px; align-items: center; }
.yummyland .showcase img { width: 100%; border-radius: 22px; box-shadow: var(--shadow-lg); }
.yummyland .showcase h2 { font-size: clamp(30px, 3.4vw, 44px); margin: 8px 0 14px; }
.yummyland .showcase h2 em { font-style: normal; color: var(--ruby); }
.yummyland .showcase p { color: var(--ink-50); font-size: 16.5px; margin-bottom: 14px; }
.yummyland .ticks { list-style: none; margin: 18px 0 28px; padding: 0; display: grid; gap: 10px; }
.yummyland .ticks li { display: flex; gap: 10px; align-items: flex-start; }
.yummyland .ticks .ico { color: #2fb57c; margin-top: 3px; }
.yummyland .price-tag { display: inline-flex; align-items: baseline; gap: 10px; margin-bottom: 18px; }
.yummyland .price-tag b { font: 800 42px/1 var(--serif); color: var(--ruby); }
.yummyland .price-tag span { font-size: 15px; font-weight: 700; color: var(--ink-50); }
.yummyland .prods { display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px; }
.yummyland .prod { display: flex; flex-direction: column; background: var(--white); border: 1px solid var(--line); border-radius: var(--r-lg); overflow: hidden; }
.yummyland .prod__art { aspect-ratio: 4 / 3; background: var(--paper); overflow: hidden; }
.yummyland .prod__art img, .yummyland .prod__art svg { width: 100%; height: 100%; object-fit: cover; }
.yummyland .prod__body { display: flex; flex-direction: column; gap: 6px; padding: 14px 16px 16px; flex: 1; }
.yummyland .prod__body b { font: 800 16px/1.2 var(--serif); }
.yummyland .prod__body p { font-size: 13px; color: var(--ink-50); flex: 1; }
.yummyland .prod__foot { display: flex; justify-content: space-between; align-items: center; margin-top: 8px; }
.yummyland .prod__foot .price { font-size: 22px; }
.yummyland .wall { display: grid; grid-template-columns: repeat(6, 1fr); gap: 14px; }
.yummyland .wall__item { display: flex; flex-direction: column; gap: 4px; font-size: 13px; }
.yummyland .wall__art { display: block; aspect-ratio: 1; border-radius: var(--r-lg); overflow: hidden; background: var(--paper); margin-bottom: 6px; }
.yummyland .wall__art img, .yummyland .wall__art svg { width: 100%; height: 100%; object-fit: cover; }
.yummyland .wall__item b { font: 800 15px/1.15 var(--serif); }
.yummyland .wall__item small { font-size: 12.5px; font-weight: 700; color: var(--pink); }
.yummyland .reviews { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; }
.yummyland .review-card { margin: 0; padding: 30px; background: var(--white); border: 1px solid var(--line); border-radius: var(--r-lg); }
.yummyland .stars { color: var(--gold); letter-spacing: .2em; }
.yummyland .review-card blockquote { margin: 12px 0 16px; font: 600 17px/1.5 var(--sans); }
.yummyland .review-card figcaption { font-size: 14px; font-weight: 800; color: var(--ink-50); }
.yummyland .faq { max-width: 780px; margin: 0 auto; border-top: 1px solid var(--line); }
.yummyland .faq details { border-bottom: 1px solid var(--line); }
.yummyland .faq summary { list-style: none; display: flex; justify-content: space-between; align-items: center; gap: 16px; padding: 20px 0; cursor: pointer; font: 800 18px/1.3 var(--serif); }
.yummyland .faq summary::-webkit-details-marker { display: none; }
.yummyland .faq summary .ico { color: var(--pink); transition: transform .25s; flex: none; }
.yummyland .faq details[open] summary .ico { transform: rotate(180deg); }
.yummyland .faq details p { padding: 0 0 22px; color: var(--ink-70); max-width: 660px; }
.yummyland .banner { display: grid; grid-template-columns: 1.3fr 1fr; gap: 40px; align-items: center; padding: 48px; border-radius: var(--r-lg); background: var(--ruby); color: var(--cream); position: relative; overflow: hidden; }
.yummyland .banner h2 { font-size: clamp(28px, 3vw, 38px); margin: 10px 0 12px; }
.yummyland .banner p { color: rgba(251, 246, 239, .78); }
.yummyland .banner .eyebrow { color: var(--gold-2); }
.yummyland .banner { border-radius: 26px; }
.yummyland .banner__list { display: grid; gap: 12px; }
.yummyland .banner__list div { display: flex; gap: 12px; align-items: center; padding: 14px 16px; border: 1px solid rgba(230, 201, 143, .3); border-radius: var(--r); font-size: 14px; letter-spacing: .06em; }
.yummyland .banner__list .ico { color: var(--gold-2); }
.yummyland .cart { display: grid; grid-template-columns: minmax(0, 1fr) 360px; gap: 40px; align-items: start; padding-top: 48px; padding-bottom: 96px; }
.yummyland .cart-item { display: grid; grid-template-columns: 220px 1fr auto; gap: 24px; padding: 24px; margin-bottom: 16px; background: var(--white); border: 1px solid var(--line); border-radius: var(--r-lg); }
.yummyland .cart-item__art--prod { border-radius: var(--r); overflow: hidden; aspect-ratio: 4 / 3; }
.yummyland .cart-item__art--prod img, .yummyland .cart-item__art--prod svg { width: 100%; height: 100%; object-fit: cover; }
.yummyland .cart-item h3 { font-size: 22px; }
.yummyland .meta { font-size: 13px; color: var(--ink-50); margin-top: 6px; }
.yummyland .meta--card { font-style: italic; font-size: 15px; color: var(--ink-70); }
.yummyland .tags { list-style: none; padding: 0; margin: 12px 0 4px; display: flex; flex-wrap: wrap; gap: 6px; }
.yummyland .tags li { display: inline-flex; align-items: center; gap: 6px; padding: 4px 10px; border-radius: 999px; background: var(--paper); font-size: 12px; }
.yummyland .cart-item__side { display: flex; flex-direction: column; align-items: flex-end; gap: 12px; }
.yummyland .cart-item__price { font: 800 24px/1 var(--serif); color: var(--ruby); }
.yummyland .qty { display: flex; align-items: center; border: 1px solid var(--line); border-radius: 999px; }
.yummyland .qty button { width: 34px; height: 34px; border: 0; background: none; display: grid; place-items: center; color: var(--ruby); }
.yummyland .qty .ico { width: 16px; height: 16px; }
.yummyland .qty span { min-width: 22px; text-align: center; font-size: 14px; }
.yummyland .cart-more { display: flex; justify-content: space-between; align-items: center; gap: 12px; margin: 8px 0 32px; }
.yummyland .panel { position: sticky; top: 96px; padding: 26px; background: var(--white); border: 1px solid var(--line); border-radius: var(--r-lg); }
.yummyland .panel__title { font-size: 24px; margin-bottom: 16px; }
.yummyland .promo { display: flex; gap: 8px; }
.yummyland .ok { color: #2e7d32; font-size: 13px; margin-top: 8px; }
.yummyland .panel .btn--block { margin-top: 20px; }
.yummyland .panel__note { display: flex; gap: 8px; align-items: center; font-size: 13px; color: var(--ink-50); margin-top: 14px; }
.yummyland .panel__note .ico { color: var(--pink); }
.yummyland .demo-note { font-size: 12px; color: var(--ink-50); text-align: center; margin-top: 12px; }
.yummyland .empty { text-align: center; padding: 64px 20px; background: var(--white); border: 1px solid var(--line); border-radius: var(--r-lg); }
.yummyland .empty .script { display: block; font: 800 34px/1.1 var(--serif); color: var(--ruby); }
.yummyland .empty p { color: var(--ink-50); margin: 10px 0 26px; }
.yummyland .prose { max-width: 720px; margin: 0 auto; }
.yummyland .prose p { color: var(--ink-70); font-size: 17px; margin-bottom: 16px; }
.yummyland .prose h2 { font-size: 32px; margin: 40px 0 14px; }
.yummyland .form { display: grid; gap: 14px; max-width: 560px; margin: 0 auto; }
.yummyland .footer { background: var(--ruby-ink); color: rgba(251, 246, 239, .72); padding: 72px 0 28px; }
.yummyland .footer .logo__txt { color: #fff; }
.yummyland .footer .logo__sub { color: rgba(255, 255, 255, .6); }
.yummyland .footer__grid { display: grid; grid-template-columns: 1.5fr repeat(3, 1fr) 1.4fr; gap: 40px; }
.yummyland .footer__brand p { font-size: 14px; margin: 18px 0 20px; max-width: 280px; }
.yummyland .footer h4 { font: 800 16px/1 var(--serif); color: var(--gold-2); margin-bottom: 16px; }
.yummyland .footer ul { list-style: none; margin: 0; padding: 0; display: grid; gap: 10px; font-size: 14px; }
.yummyland .footer a { text-decoration: none; }
.yummyland .footer a:hover { color: var(--gold-2); }
.yummyland .footer__note { font-size: 14px; margin-bottom: 14px; }
.yummyland .socials { display: flex; gap: 8px; }
.yummyland .socials a { width: 38px; height: 38px; border-radius: 50%; border: 1px solid rgba(230, 201, 143, .3); display: grid; place-items: center; }
.yummyland .socials .ico { width: 17px; height: 17px; }
.yummyland .subscribe { display: flex; gap: 6px; }
.yummyland .subscribe input { background: rgba(255, 255, 255, .06); border-color: rgba(230, 201, 143, .3); color: var(--cream); }
.yummyland .subscribe button { padding: 0 18px; border: 0; border-radius: 999px; background: var(--gold); color: var(--ink); font-size: 14.5px; font-weight: 800; }
.yummyland .footer__bottom { display: flex; justify-content: space-between; gap: 16px; flex-wrap: wrap; margin-top: 56px; padding-top: 22px; border-top: 1px solid rgba(230, 201, 143, .18); font-size: 12.5px; }
.yummyland .footer__bottom nav { display: flex; gap: 20px; }
.toast {
  position: fixed; left: 50%; bottom: 24px; z-index: 100; display: flex; align-items: center; gap: 10px;
  max-width: calc(100% - 32px); padding: 13px 20px; border-radius: 999px; background: var(--ruby-ink); color: var(--cream);
  font-size: 14px; box-shadow: var(--shadow-lg); transform: translate(-50%, 140%); opacity: 0; transition: all .35s var(--ease);
}
.toast .ico { color: var(--gold); width: 18px; height: 18px; }
.toast.is-on { transform: translate(-50%, 0); opacity: 1; }
@media (max-width: 1080px) {.yummyland .nav { gap: 18px; }
.yummyland .cols { grid-template-columns: repeat(2, 1fr); }
.yummyland .occs { grid-template-columns: repeat(2, 1fr); }
.yummyland .prods { grid-template-columns: repeat(2, 1fr); }
.yummyland .wall { grid-template-columns: repeat(4, 1fr); }
.yummyland .footer__grid { grid-template-columns: 1fr 1fr 1fr; }
.yummyland .footer__brand { grid-column: 1 / -1; }}
@media (max-width: 900px) {.yummyland .burger { display: grid; }
.yummyland .nav {
    position: absolute; top: 100%; left: 0; right: 0; flex-direction: column; gap: 0; margin: 0;
    background: var(--cream); border-bottom: 1px solid var(--line); padding: 8px 20px 18px; display: none;
  }
.yummyland .nav.is-open { display: flex; }
.yummyland .nav a { padding: 14px 0; border-bottom: 1px solid var(--line); }
.yummyland .header__tools { margin-left: auto; }
.yummyland .hero__in { grid-template-columns: minmax(0, 1fr); gap: 40px; padding-top: 56px; padding-bottom: 56px; }
.yummyland .hero__stamp { left: auto; right: 10px; bottom: -22px; width: 96px; height: 96px; font-size: 16px; }
.yummyland .trust { grid-template-columns: 1fr 1fr; }
.yummyland .trust__item:nth-child(3) { border-left: 0; }
.yummyland .trust__item:nth-child(n+3) { border-top: 1px solid var(--line); }
.yummyland .builder__grid { grid-template-columns: minmax(0, 1fr); gap: 28px; }
.yummyland .bprev { position: static; }
.yummyland .showcase { grid-template-columns: 1fr; gap: 36px; }
.yummyland .reviews { grid-template-columns: 1fr; }
.yummyland .banner { grid-template-columns: 1fr; padding: 36px 28px; }
.yummyland .cart { grid-template-columns: 1fr; }
.yummyland .panel { position: static; }}
@media (max-width: 640px) {.yummyland .wrap { padding: 0 16px; }
.yummyland .header__in { height: 62px; gap: 12px; }
.yummyland .logo__txt { font-size: 24px; }
.yummyland .logo__sub { font-size: 11px; }
.yummyland .announce { font-size: 12.5px; }
.yummyland .section { padding: 64px 0; }
.yummyland .section__head { margin-bottom: 32px; }
.yummyland .hero h1 { font-size: 38px; }
.yummyland .hero__lede { font-size: 16px; }
.yummyland .hero__cta .btn { flex: 1 1 100%; }
.yummyland .hero__facts { gap: 18px; }
.yummyland .hero__facts b { font-size: 22px; }
.yummyland .trust__item { padding: 16px 12px; gap: 10px; }
.yummyland .trust__item span { font-size: 12px; }
.yummyland .cols, .yummyland .occs { grid-template-columns: 1fr; }
.yummyland .occs { grid-template-columns: 1fr 1fr; gap: 12px; }
.yummyland .occ-card { padding: 18px 10px; }
.yummyland .occ-card__pola { width: 92px; }
.yummyland .occ-card b { font-size: 17px; }
.yummyland .prods { grid-template-columns: 1fr 1fr; gap: 10px; }
.yummyland .wall { grid-template-columns: repeat(3, 1fr); gap: 10px; }
.yummyland .how { grid-template-columns: 1fr; }
.yummyland .how__item + .how__item { border-left: 0; padding-left: 0; border-top: 1px solid var(--line); }
.yummyland .how__item { padding: 22px 0; }
.yummyland .builder { padding: 40px 0 64px; }
.yummyland .bsteps-wrap { top: 62px; margin: 0 0 24px; }
.yummyland .bsteps-wrap .wrap { padding: 0 4px; }
.yummyland .bsteps__btn { flex-direction: column; gap: 4px; padding: 10px 2px; font-size: 12.5px; }
.yummyland .bprev__bar { padding: 12px 14px; }
.yummyland .bprev__price { font-size: 24px; }
.yummyland .bstep { min-height: 0; }
.yummyland .occ-grid { grid-template-columns: repeat(4, 1fr); gap: 6px; }
.yummyland .occ { padding: 10px 2px; }
.yummyland .occ b { font-size: 12px; }
.yummyland .candies { gap: 10px; }
.yummyland .candy__body { padding: 10px 10px 12px; }
.yummyland .candy__body b { font-size: 16px; }
.yummyland .candy__body p { display: none; }
.yummyland .candy__body .btn { width: 100%; padding: 0 8px; margin-top: 6px; }
.yummyland .tray-mobile { display: block; position: sticky; top: 124px; z-index: 20; margin: 0 -16px 8px; padding: 10px 16px; background: rgba(255, 241, 238, .96); backdrop-filter: blur(8px); border-bottom: 1px solid var(--line); }
.yummyland .tray { container-type: inline-size; gap: 6px; padding: 8px; border-radius: 8px; background: var(--ruby); }
.yummyland .tray .cup { aspect-ratio: 5 / 3; border-radius: 6px; border-width: 1px; }
.yummyland .tray .cup__fill { inset: 2px; border-radius: 4px; }
.yummyland .tray .cup__plus { font: 500 14px/1 var(--serif); }
.yummyland .tray .cup.is-selected { box-shadow: 0 0 0 2px var(--gold-2); }
.yummyland .choice, .yummyland .uploads, .yummyland .fields--2 { grid-template-columns: 1fr; }
.yummyland .uploads { grid-template-columns: 1fr 1fr; }
.yummyland .touch { grid-template-columns: 96px 1fr; }
.yummyland .lidopts { grid-template-columns: 1fr 1fr; gap: 8px; }
.yummyland .lidopt--main { flex-direction: column; align-items: stretch; }
.yummyland .lidopt--main .lidopt__art { padding: 10px 10px 0; }
.yummyland .lidopt__art { padding: 8px; }
.yummyland .lidopt__body { padding: 10px 10px 12px 38px; }
.yummyland .lidopt__radio { left: 10px; top: 11px; }
.yummyland .lidopt__body > b { font-size: 15px; }
.yummyland .lidopt__body > small { font-size: 12.5px; }
.yummyland .upsell { grid-template-columns: 1fr; }
.yummyland .closed { padding: 16px; }
.yummyland .included ul { grid-template-columns: 1fr; }
.yummyland .review__cups { grid-template-columns: 1fr; }
.yummyland .bnav .btn { padding: 0 16px; }
.yummyland .gpick { grid-template-columns: repeat(2, 1fr); }
.yummyland .modal__box { padding: 22px 16px; }
.yummyland .cart-item { grid-template-columns: 1fr; gap: 14px; padding: 16px; }
.yummyland .cart-item__side { flex-direction: row; align-items: center; justify-content: space-between; }
.yummyland .footer__grid { grid-template-columns: 1fr 1fr; gap: 28px; }
.yummyland .footer__grid > div:last-child { grid-column: 1 / -1; }}
@media (prefers-reduced-motion: reduce) {.yummyland *, .yummyland *::before, .yummyland *::after { animation: none !important; transition: none !important; scroll-behavior: auto !important; }}