# Перенос конструктора на Shopify

## Что уже сделано в магазине YUMMYLAND

Выполнено через Admin API 1 августа 2026. Живая тема **Nook не тронута**.

| Что | Состояние |
|---|---|
| Товар **Build Your Own Candy Box** (4 варианта по размерам, SKU `YL-BOX-*`) | создан, **draft** |
| 5 платных допов (`YL-EXTRA-*`) | созданы, **draft** |
| 6 наценок за премиум-конфеты (`YL-PREMIUM-*`) | созданы, **draft** |
| Коллекция **Box add-ons** (`box-add-ons`, умная, по тегу `yl-addon`) | создана, 11 товаров |
| Тема **Nook + Box Builder** (дубликат Nook) | создана, **не опубликована** |
| 14 файлов темы (assets, 2 секции, шаблон страницы) | залиты, размеры сверены |
| Страница **/pages/build-your-box** с шаблоном `build-your-box` | создана |

> ### ⚠️ Товары устарели после перехода на модель «по весу»
>
> Те 12 draft-товаров создавались под старую слотовую модель. Сейчас в `data.js` другие
> цены, другие варианты и другой список премиум-позиций, поэтому **перед активацией их
> надо привести в соответствие** — иначе конструктор посчитает одну цену, а корзина
> возьмёт другую. Ничего покупателю не видно: все 12 в статусе draft.
>
> Что разошлось:
>
> | Товар | В магазине сейчас | Должно быть |
> |---|---|---|
> | `YL-BOX-SMALL` | «up to 4 candies», $24.99 | «1 lb, 4 scoops», **$22.99**, 454 г |
> | `YL-BOX-MEDIUM` | «up to 7 candies», $34.99 | «2 lb, 8 scoops», **$36.99**, 907 г |
> | `YL-BOX-LARGE` | «up to 10 candies», $49.99 | «3 lb, 12 scoops», **$52.99**, 1361 г |
> | `YL-BOX-PARTY` | «up to 15 candies», $69.99 | «5 lb, 20 scoops», **$79.99**, 2268 г |
> | `YL-EXTRA-SCOOP` | «Extra Candy Scoop», $4.99 | «Extra 4 oz Scoop», **$5.99** |
> | 6 шт. `YL-PREMIUM-*` | choc-almonds, choc-pretzels, choc-peanuts, choc-truffles, freeze-rainbow, freeze-taffy | этих конфет больше нет в каталоге — товары осиротели |
> | 16 новых `YL-PREMIUM-*` | отсутствуют | нужны под новые премиум-совки |
>
> Самый быстрый способ: импортировать заново сгенерированный `shopify/products.csv`
> (Products → Import). Shopify матчит по `Handle`, так что бокс и допы обновятся на месте,
> а 16 премиум-совков создадутся. Шесть осиротевших `yl-premium-*` импорт не тронет —
> их надо удалить руками (они draft, спешить некуда). После импорта проверить,
> что все нужные товары лежат в коллекции `box-add-ons` (она умная, по тегу `yl-addon`,
> так что подхватит сама).

Осталось сделать руками:

0. **Обновить товары** — см. блок выше. Без этого дальше идти нет смысла.
1. Открыть превью темы «Nook + Box Builder» и посмотреть страницу `/pages/build-your-box`.
2. В редакторе тем у секции **Yummyland box builder** проверить, что выбраны
   товар *Build Your Own Candy Box* и коллекция *Box add-ons*
   (в шаблоне они уже прописаны по handle, но настройки стоит подтвердить глазами).
3. **Перевести товары в Active** (после обновления их станет 22) — пока они draft, витрина их не видит, и кнопка
   «Add to cart» работать не будет. Это единственный шаг, без которого превью неполное.
4. Добавить ссылку «Build Your Box» в меню и кнопку на главной.
5. Когда всё нравится — Publish темы. Откат: опубликовать Nook обратно.

Проверить, что связка сошлась, проще всего так: собрать бокс с премиум-совком и платным
допом и посмотреть корзину — там должны появиться три позиции (бокс + доп + наценка),
а сумма совпасть с той, что показывал конструктор. Заодно посмотрите свойства позиции:
в них должен быть состав с весом каждого совка и общий вес заполнения.

---

## Если магазин уже есть (наш случай: YUMMYLAND, yummylandcandy.com, план Basic)

**Второй магазин не нужен.** Отдельный магазин заводят только если это другой бренд,
другое юрлицо, другая валюта или отдельный домен — здесь всё то же самое, значит работаем
в существующем. Второй магазин = вторая подписка, разделённые заказы, клиенты и аналитика.

Работаем с копией темы, живой сайт при этом не трогается:

1. **Online Store → Themes → ⋯ → Duplicate** на текущей теме.
2. Все файлы из `shopify/` заливаем **в копию** (Edit code или `shopify theme push --theme <id>`).
3. Смотрим через **Preview** — покупатели этого не видят.
4. Когда всё нравится — **Publish**. Откат в один клик: опубликовать обратно старую тему.

Дальше два сценария, оба в одном магазине:

| | Что делаем | Когда выбирать |
|---|---|---|
| **A. Конструктор как страница** | Добавляем страницу `/pages/build-your-box`, ссылку в меню и кнопку на главной | Текущий сайт устраивает, нужен только конструктор |
| **B. Лендинг становится главной** | То же плюс секции `Yummyland box builder` + `Yummyland marketing` на Home в редакторе тем | Хотим, чтобы главная выглядела как этот лендинг |

В обоих случаях шапка, футер, меню, корзина, чекаут, скидки и почтовые уведомления остаются
темины и шопифаевские — мы их не подменяем. Наш CSS заскоупен под `.yummyland`,
поэтому остальной магазин выглядит как раньше.

### Что переносится, а что нет

| Из статики | На Shopify |
|---|---|
| Конструктор (`index.html` + `builder.js`) | секция `yummyland-builder` — переносится целиком |
| Блоки «как это работает», боксы, поводы, отзывы, FAQ | секция `yummyland-marketing` — переносится целиком |
| Шапка, футер, меню (`site.js`) | **не переносим** — берём из темы |
| Корзина `cart.html`, промокоды, расчёт доставки | **не переносим** — родная корзина, Discounts и Shipping |
| `about.html`, `faq.html` | обычные страницы Shopify, текст берём из `assets/js/data.js` (`YL.FAQ`) |

### Про план Basic

Наценки за премиум-конфеты и платные допы добавляются отдельными позициями в корзину —
это единственный способ на Basic (Shopify Functions и Scripts доступны только на Plus).
Схема рабочая и прозрачная: покупатель видит, за что доплачивает, а сборщик заказа —
полный состав бокса в свойствах позиции.

Чтобы допы не выглядели в корзине мусором, в теме можно скрыть строки с тегом `yl-addon`
или показывать их вложенными под боксом (группировка идёт по свойству `_yl_group`).

---


Этот каталог — готовый «drop-in» для любой темы Shopify 2.0 (Dawn и её производные).
Конструктор встраивается секцией, корзина и оплата — родные шопифаевские.

Всё в `shopify/assets/` генерируется из исходников командой:

```bash
node tools/build-shopify.js
```

Правим всегда исходники в `assets/`, потом пересобираем. Руками в `shopify/assets/` не редактируем.

---

## Как это устроено

Shopify не умеет считать цену «сконструированного» товара на лету, поэтому используется
стандартная и надёжная схема:

| Что | Как попадает в корзину |
|---|---|
| Сам бокс | товар **Build Your Own Candy Box**, вариант = вес (1 lb / 2 lb / 3 lb / 5 lb) |
| Состав бокса **с весом каждого совка**, общий вес заполнения, вайб, открытка | **line item properties** на этой позиции (видны в корзине, чеке и на упаковочном листе — упаковщик отмеряет прямо по ним) |
| Платные допы (стикеры, упаковка…) | отдельные позиции-товары `yl-extra-*` |
| Наценка за премиум-совки | отдельные позиции-товары `yl-premium-*`, по одной на каждый совок |

Все позиции одного бокса помечаются скрытым свойством `_yl_group`, так что в админке видно,
что относится к одному боксу. Свойства с `_` в начале покупателю не показываются.

Полный состав бокса дополнительно кладётся в `_yl_payload` в JSON — удобно для приложений
печати упаковочных листов и для аналитики.

---

## Пошагово

### 1. Импорт товаров

1. Admin → **Products → Import** → загрузить `shopify/products.csv`.
   Создаётся 1 товар с 4 вариантами по весу + 21 товар-доп (5 платных допов и 16 премиум-совков).
2. Создать коллекцию **Box add-ons** (handle `box-add-ons`), тип — automated,
   условие: `Product tag equals yl-addon`. Туда попадёт весь 21 доп.
3. Чтобы допы не мозолили глаза в каталоге: в остальных коллекциях добавить условие
   `Product tag is not equal to yl-addon`, а в теме исключить их из поиска
   (или поставить им статус «доступен только по прямой ссылке» через Sales channels).

Цены в CSV взяты из `assets/js/data.js` — если меняешь цены, меняй их там и пересобирай CSV,
тогда витрина и корзина не разъедутся.

### 2. Загрузка файлов темы

Admin → **Online Store → Themes → ⋯ → Edit code**:

- всё из `shopify/assets/` → в папку **Assets** темы
  (включая `.woff2` — их можно загрузить как обычные ассеты);
- `shopify/sections/yummyland-builder.liquid` и `yummyland-marketing.liquid` → в **Sections**;
- `shopify/templates/page.build-your-box.json` → в **Templates**.

Быстрее через Shopify CLI из корня темы:

```bash
shopify theme dev            # локальный превью
shopify theme push --only "assets/yummyland-*" --only "sections/yummyland-*" --only "templates/page.build-your-box.json"
```

### 3. Страница конструктора

1. Admin → **Pages → Add page**, заголовок «Build Your Box», handle `build-your-box`.
2. Справа в **Theme template** выбрать `build-your-box`.
3. Открыть страницу в редакторе тем → в секции **Yummyland box builder** выбрать:
   - **Box product** → Build Your Own Candy Box
   - **Add-ons collection** → Box add-ons
4. Сохранить. Готово — конструктор работает, «Add to cart» кладёт бокс в родную корзину.

Размеры сопоставляются с вариантами **по названию** (ищутся слова small / medium / large / party),
поэтому ID вручную вписывать не нужно. Если варианты названы иначе — они возьмутся по порядку.

### 4. Главная страница

Секции `Yummyland box builder` и `Yummyland marketing` доступны и на главной:
Customize → Add section → выбрать их. Хедер, футер, меню и корзина остаются темины.

---

## Что важно знать

- **CSS изолирован.** Все стили в `yummyland-style.css.liquid` заскоупены под `.yummyland`,
  тема не поедет. Обратная сторона: markup конструктора обязан лежать внутри `<div class="yummyland">` —
  в секциях это уже так.
- **Корзина.** `assets/js/shopify-cart.js` подменяет `YL.addToCart`. Если товар-доп не найден
  в коллекции, бокс всё равно добавится, но наценка не спишется — в консоли будет предупреждение
  со списком недостающих handle. Проверь это после импорта на тестовом заказе.
- **Промокоды и доставка** со статичной версии (`SWEET10`, бесплатно от $50) на Shopify не нужны —
  настраиваются в Discounts и Shipping. Блок доставки в превью показывает «Calculated at checkout».
- **Страницы `cart.html`, `about.html`, `faq.html`** в тему не переносятся: корзина у Shopify своя,
  а About/FAQ проще сделать обычными страницами и перенести текст из `assets/js/data.js` (`YL.FAQ`).
- **Аналитика.** После успешного добавления бросается событие `yummyland:added`
  с составом бокса в `detail.box` — можно повесить GA4/Meta Pixel.

---

## Альтернатива: оставить как есть

Если Shopify пока не нужен, сайт полностью самодостаточен: это статика без сборки.
`README.md` в корне описывает деплой на GitHub Pages / Netlify / Vercel (конфиги уже лежат).
Корзина в этом случае живёт в браузере, а оформление заказа — демонстрационное:
принимать деньги без платёжного провайдера нельзя.

Разумный промежуточный вариант — оставить лендинг на статике, а кнопку «Add to cart»
увести в Shopify через тот же адаптер (Storefront API + permalink `/cart/<variant>:<qty>`),
если хочется свой фронт и шопифаевский чекаут.
